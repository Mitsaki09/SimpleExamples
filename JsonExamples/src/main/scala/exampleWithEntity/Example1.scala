package exampleWithEntity

import de.heikoseeberger.akkahttpcirce.FailFastCirceSupport._
/*
case class Jopa(size: Int, color: String)
object Jopa {
  implicit val encoder: Encoder[Jopa] = deriveEncoder[Jopa]
  implicit val decoder: Decoder[Jopa] = deriveDecoder[Jopa]
}

path("update" / "jopa") {
  (post & entity(as[Jopa])) { myJopa =>

  // доступны поля жопы
  myJopa.size
  myJopa.color

  complete("OK")
}
}*/
